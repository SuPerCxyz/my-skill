# docs

本目录存放 superpowers 工作流产生的 plan / spec 设计文档存档，**并非 skill**，仅作历史参考留档。

## 说明

- `plans/` — 实施计划文档
- `specs/` — 设计规格文档

这些文档记录了 `easystack-cloud-web-e2e` skill 开发过程中的设计决策与重构计划。文档中的相对路径可能引用旧目录名(如 `easystack-ui-test/`，现已更名为 `easystack-cloud-web-e2e/`)，链接可能失效，请以实际 skill 目录为准。