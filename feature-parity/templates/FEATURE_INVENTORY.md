# Feature Inventory

Reference revision: `<repo>@<commit>`  
Target baseline: `<repo>@<commit>`  
Scope: `<scope>`

## Feature tree

### F-001 — <major capability>
Reference surfaces: `<UI/API/CLI/config/job/...>`

- **F-001.1 — <subfeature>**
  - **F-001.1a — <atomic observable behavior>**
    - Trigger/entry:
    - Preconditions/visibility:
    - Inputs/defaults/validation:
    - State transition:
    - Side effects/persistence:
    - Output/feedback:
    - Failure/retry/timeout/cancel:
    - Edge cases:
    - Reference evidence:
    - Acceptance criterion:

## Suspected but unverified
- `<candidate>` — clue: `<doc/screenshot/issue>` — verify via: `<source/runtime path>`
